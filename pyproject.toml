[project]
name = "backend"
version = "0.1.0"
description = "Hermes Agent cloud deployment package"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hermes-agent @ git+https://github.com/NousResearch/hermes-agent.git@v2026.5.7",
]
